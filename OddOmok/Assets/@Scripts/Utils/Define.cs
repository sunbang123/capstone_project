using UnityEngine;

public static class Define
{
    public enum EScene
    {
        TitleScene,
        GameReadyScene,
        GameScene,
    }

    public enum EUIEvent
    {
        Click,
        PointerDown,
        PointerUp,
        Drag,
    }

    public enum ESound
    {
        Bgm,
        Effect,
        Max,
    }
}
